Git Branch Experiment:

This project demonstrates the creation and management of five Git branches. Each branch contains a separate module of the project to show how parallel development is performed in Git. The project also demonstrates branch creation, switching, and pushing to a remote repository.
